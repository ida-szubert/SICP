#lang racket

;Exercise 4.31
;it might be a good idea to implement lazy-evaluation as an upward-compatible extension
;so that ordinary Scheme programs will continue to work as intended
;the user might decide, upon defining a procedure, whether the arguments are to be delayed or not
;as well as wether to use memoization or not
;(define (foo a (b lazy) c (d lazy-memo))

;TODO

